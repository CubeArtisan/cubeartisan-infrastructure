apiVersion: v1
kind: Service
metadata:
  labels:
    service: mtgml-server
  name: mtgml-server
  namespace: mtgml
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8000
  selector:
    service: mtgml-server
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: mtgml-server
  name: mtgml-server
  namespace: mtgml
spec:
  revisionHistoryLimit: 1
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
  selector:
    matchLabels:
      service: mtgml-server
  template:
    metadata:
      labels:
        service: mtgml-server
    spec:
      restartPolicy: Always
      imagePullSecrets:
        - name: github-docker
      serviceAccountName: log-and-trace
      containers:
        - name: cube-recommender
          image: "ghcr.io/cubeartisan/mtgml:20220502-3"
          env:
            - name: MTGML_VERSION
              value: "20220502-3"
          ports:
            - containerPort: 8000
              name: http
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              memory: 3Gi
              cpu: 1
            requests:
              memory: 2Gi
              cpu: 1
          livenessProbe:
            httpGet:
              path: /version
              port: http
            failureThreshold: 1
            periodSeconds: 60
          readinessProbe:
            httpGet:
              path: /version
              port: http
            initialDelaySeconds: 15
            periodSeconds: 60
          startupProbe:
            httpGet:
              path: /version
              port: http
            failureThreshold: 15
            periodSeconds: 60
